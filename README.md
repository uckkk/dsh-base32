# dsh-base32 · Base32 编解码（RFC 4648）

纯 Node 实现，无网络、无外部服务。

## 安装

```bash
dsh plugin add dsh-base32
```
安装后在 profile 的 `package.json` 的 `dsh.profile.bundles` 中加入 `"dsh-base32"`。

## 安装

```bash
dsh plugin add github:uckkk/dsh-base32
```

## 使用

安装后在会话中调用该插件注册的工具即可。
